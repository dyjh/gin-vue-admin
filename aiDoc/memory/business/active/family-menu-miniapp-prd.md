# 家庭点餐微信小程序 PRD

## 基本信息

- 提出日期：2026-07-07
- 当前状态：`active`
- 需求类型：产品 PRD / 微信小程序 / 熟人饭局点餐
- 优先级：高
- 需求文件：`aiDoc/memory/business/active/family-menu-miniapp-prd.md`
- PRD 文件：`aiDoc/prd/family-menu-miniapp-prd.md`

## 用户原始意图摘要

用户希望基于已初始化的框架，设计一个面向家庭和熟人饭局的点餐小程序，产品名确定为“来干饭”：用户在小程序维护个人菜品库、菜谱和饭局，家人或朋友通过点餐码参与点菜，系统生成采购清单，并通过 AI 和积分体系提升菜品配置与做菜规划效率。

## 影响范围

- 后端：小程序用户、菜品、菜谱、饭局、采购清单、积分、AI、推荐池、内容安全、通知等业务模块。
- 前端：微信小程序新客户端；后台管理端新增配置和运营页面。
- 文档：新增 PRD；后续实现前需继续拆分技术方案和实施计划。
- 插件 / 模块：建议按独立业务模块接入，不污染系统核心模块。

## 涉及对象

- 模块：菜品、菜谱、饭局、采购清单、积分、AI、推荐、内容安全、通知。
- 接口：微信登录、菜品 CRUD、菜谱 CRUD、饭局点餐、采购清单、积分流水、AI 调用、推荐内容、违规处理。
- 页面：小程序首页、菜谱、我的、菜品详情、饭局、采购清单、通知中心；后台 AI 配置、积分管理、分类单位、推荐池、内容安全。
- 配置：AI 供应商、模型、平台总开关、积分消耗、限额、固定分类、固定单位。

## 历史访谈约束与演进记录

本节保留早期访谈和方案演进，用于追溯决策，不作为当前实现依据。与后文“现行产品口径”、主 PRD、跨端契约基线冲突时，以后者为准。

- 第一版采用个人空间模式，不做家庭成员体系。
- 小程序是用户配置菜品和菜谱的主入口，后台不替普通用户维护私有菜品。
- 管理后台负责 AI 接入、固定分类、固定单位、积分、推荐、内容安全和数据展示。
- 第一版完整覆盖当前 PRD 中的核心闭环、AI、积分、推荐和违规处理能力。
- 参与饭局和创建饭局都必须微信登录。
- 点餐码创建时选择有效期；创建者可提前关闭点菜。
- 点菜规则为参与者表达想吃，系统统计想吃人数，创建者最终确认制作份数。
- 菜品分类第一版使用后台固定类目，后续预留用户自定义分类。
- 菜品标签第一版由后台固定配置，用户自己的菜品最多选择 3 个标签，后台可按标签筛选。
- 菜品状态包括草稿、可用、已下架；新建菜品默认草稿，保存为可用后才可加入菜谱、饭局和点餐流程。
- 菜品采用单实体模型，不维护菜品版本表；草稿只用于新建未完成、AI 解析未确认、打卡识别未确认。
- 可用私有菜品编辑后直接保存生效，不生成单独草稿；已下架菜品可直接恢复为可用状态。
- 已公开菜品编辑后自动取消公开，保存为可用私有菜品；再次公开需重新申请公开审核。
- 饭局候选菜使用独立候选菜 ID 稳定承载点餐选项；点餐阶段不保存完整菜品快照，确认最终菜单时才生成独立饭局菜品快照，包含完整做法、配料、基础份量和最终制作份数；饭局快照不依赖菜品历史版本。
- 用户可在小程序主动下架自己的菜品；下架不删除历史数据，已公开菜品下架时同步取消公开并退出推荐池。
- 私有菜品没有发布概念，保存为可用菜品不审核；审核只发生在用户主动公开菜品时。
- 后台可配置菜品公开审核策略：无需审核即可公开、人工审核后公开；第一版不存在公开内容自动审核逻辑，图片内容安全放在图片上传阶段同步拦截。
- 人工审核通过后菜品公开，驳回后保持可用私有状态并通知用户原因；无需审核策略下，用户打开公开开关后直接公开。
- 用户打开公开开关时自动发起公开审核；审核中菜品不可编辑，用户可取消公开申请后再编辑；审核通过后的公开菜品被编辑时自动关闭公开并退出推荐池，再次公开需重新审核。
- 配料使用配料名、数量、单位、备注；单位由后台固定维护。
- 菜品图片可为空；用户不上传也不使用 AI 生图时，展示后台配置的默认占位图。
- 图片统一使用图片资源模型，保存图片地址或对象存储 Key、来源场景、上传用户、阿里云同步审查状态和最近审查记录 ID；菜品、打卡、AI 生图等业务对象引用图片资源。
- 菜谱第一版为菜品合集 + 自然语言备注，预留偏好规则和 AI 推荐扩展。
- 采购清单采用简洁可用方案，有采购清单主记录和清单项；清单项只区分未购买和已购买，支持勾选、编辑、新增、删除、复制或分享；第一版不做采购主状态、价格、库存、超市货架分类、多人实时协同和结算。
- AI 支持菜品文本解析、菜谱长截图解析、菜品图片生成、饭局做菜规划、打卡照片识别菜品草稿。
- 菜谱长截图解析要求绑定具备图片理解能力的模型。
- AI 供应商第一版固定支持千问（阿里云百炼）、DeepSeek、GPT（OpenAI）；第三方中转或任意 OpenAI 兼容接口不在首期范围。
- AI 模型由后台下拉选择，小程序端不暴露模型选择。
- 每个 AI 能力都有独立后台配置页，用于配置积分、模型、提示词和限额；固定结果校验由服务端强制执行并只读展示，不提供可编辑策略 JSON，启停只由平台总开关控制。
- 积分第一版用于 AI 功能，主要来源为做菜打卡和后台人工发放，预留充值、签到、任务等完整体系。
- AI 第一版按能力固定积分扣费，后台记录 token 和图片成本，预留 token/hybrid 扣费模式。
- AI 调用记录保存原始输入文本、图片摘要和图片元信息、模型返回内容、调用状态、token 和积分流水；原始输入文本、图片摘要、图片元信息和模型返回内容默认长期保留，管理员可在后台手动清除这些内容，清除后保留必要审计字段。
- 外部模型调用失败或超时时，AI 失败并新增退还积分流水；成功生成但用户不采用不退积分。
- 用户菜品默认私有，主动公开并通过公开审核后才可进入推荐池。
- 用户公开菜品前不需要单独授权弹窗，但公开操作页需展示公开范围说明。
- 首页可展示后台精选公开菜品，用户一键加入时复制到自己的菜品库。
- 平台推荐内容复制到用户菜品库时默认成为可用私有菜品，不重新走公开审核；用户后续主动公开时才按审核策略处理。
- 管理员可在后台创建推荐菜谱；用户一键加入时复制为自己的菜谱和菜品副本。
- 做菜打卡作为第一版能力，入口在“我的”，以日历方式展示，支持拍照和输入菜名。
- 打卡允许每天多次，但每天仅首次打卡奖励积分。
- 打卡可选 AI 识别能力：用户主动勾选后，系统识别打卡照片并生成用户自己的私有菜品草稿，消耗积分，不自动公开。
- 图片内容审查第一版适配阿里云图片内容安全同步审查接口，用户图片上传时同步调用；通过后才生成可用图片资源，违规或疑似风险时提示图片违规并上传失败，接口异常或超时时提示稍后重试。
- 打卡照片 AI 识别复用上传阶段的阿里云图片同步审查结果；能进入打卡的图片资源默认已通过审查，上传失败时不调用 AI、不扣分。
- 涉及公开展示或推荐池的图片必须有阿里云同步审查通过结果；上传审查失败的图片不能进入业务对象。
- 打卡数据用于维护用户做菜偏好画像。
- “不知道吃什么”AI 推荐作为第一版 AI 增强能力，至少累计打卡 7 天后可用；同一用户同一自然日免费推荐最多 2 次，超出后可按后台配置消耗积分。
- 饭局创建后，候选源菜品被删除或不可用时保留候选记录并展示失效占位，但不可继续点选、已有票数不计入统计且不能进入最终菜单；创建者主动移除候选时同样不再展示或计票。
- 饭局状态不单独使用过期终态；点餐码过期会把收集中的饭局关闭，关闭原因为过期关闭，关闭后不能重新开启；同一个微信用户不能重复加入同一个饭局。
- 创建者修改制作份数后，AI 做菜规划仍是可选能力，每次生成按后台配置消耗积分。
- 用户只有一个当前积分字段，不单独维护积分账户表；AI 扣费采用请求幂等号 `requestId` + 用户当前积分字段数据库事务锁方案，扣减和退还积分时锁定用户当前积分字段所在行；用户维度 mutex 可作为性能优化，但不能作为唯一正确性保障。
- `orderfood` 持久化模型必须使用完整 GORM 列定义；业务表统一采用不超过 30 个字符的 `of_` 短表名；结构体字段说明放在字段同行，方法和关键业务逻辑必须有解释性注释。
- 菜品表保存当前公开状态、当前审核状态和最近公开审核记录 ID；公开审核记录表保存每次申请、取消与审核历史。
- 公开审核记录保存申请时的内容摘要或内容快照，用于确认审核依据；用户取消公开申请时记录已取消状态、取消用户和取消时间。
- 图片内容审查记录允许上传阶段业务对象类型和业务对象 ID 为空，后续业务保存时通过图片资源 ID 绑定到具体业务对象。
- 同一饭局、同一参与者、同一候选菜只保留一条点菜记录，反复点击只更新“是否想吃”和点选时间。
- “不知道吃什么”推荐记录需标记是否占用免费次数，用于约束同一用户同一自然日免费推荐最多 2 次。
- 管理员推荐菜谱需要独立平台推荐菜谱和推荐菜谱菜品项模型，一键加入时复制为用户自己的菜谱和菜品副本。
- 菜品删除需记录删除状态、删除原因、删除操作者和违规类型；管理员严重违规删除公开源菜品时可级联删除复制链。
- 产品核心指标包括新用户首次创建菜品率、菜品点餐率、人均创建菜品数、AI 解析使用率、AI 结果采用率、点餐码参与率、饭局创建成功率、采购清单生成率、打卡率、推荐一键加入率和留存率。
- 用户主动使用 AI 功能时，相关输入内容会提交给后台配置的 AI 服务商处理；小程序隐私协议需覆盖 AI、图片、公开菜品、打卡、违规处理、行为数据、数据长期保留和后台清除能力。
- 第一版小程序侧不提供用户自助注销账号能力；用户账号、积分流水、饭局历史、点餐记录、采购清单、打卡记录、违规处理记录和必要操作日志默认长期保留。
- 用户删除自己的菜品时同步删除该菜品绑定的图片资源数据和对象存储文件；已确认饭局和采购清单保留必要文字快照，但不继续依赖被删除菜品的图片资源。
- 管理员可处理违规内容、删除菜品、禁用用户，并通知用户；用户被禁用后不能登录小程序使用核心功能，既有内容不自动删除或下架，但其发起的 `collecting/closed/confirmed` 饭局同步以 `creator_disabled` 原因取消，已确认快照和采购清单保留只读并通知参与者。
- 第一版微信订阅消息只保留固定场景 `meal_status`（饭局最终结果），用于参与者主动授权后的确认或取消结果；关闭点餐、菜品审核、用户禁用和 AI 退款只写站内通知，不发送微信订阅消息。
- 图片上传阶段的阿里云同步审查失败当场返回错误，不生成通知中心记录或微信订阅消息；“违规处理结果通知”只用于已保存业务内容后续被管理员取消公开、下架、删除、复制链删除等处理后，通知受影响用户处理结果和原因，它不是订单通知，第一版不作为独立微信订阅消息模板。
- 严重违规删除公开源菜品时可级联删除复制链；普通下架不级联；用户自删不影响复制品。

## 现行产品口径（覆盖历史描述）

以下口径汇总至 2026-07-25，若与上方早期访谈记录冲突，以本节、当前 PRD 和跨端契约基线为准：

- 用户菜品使用“允许被发现 / 未公开”口径，不设置单独的公开申请或公开审核流程；管理员挑选进入推荐即构成平台审核行为。
- 菜品封面图必选，菜品简介选填；做法步骤支持纯文字，也支持文字下方配大图。
- 菜品状态统一为 `draft|usable`；用户删除使用软删除和审计字段，不再设计“已下架”用户状态。
- 饭局状态统一为 `collecting|closed|confirmed|completed|cancelled`；截止时间到达时进入 `closed` 并记录到期关闭原因，不使用 `expired` 终态。`confirmed` 表示采购进行中，创建者手动完成后进入 `completed`。
- 同一创建者最多存在一个 `collecting|closed|confirmed` 饭局，只有完成或取消后才能创建下一场；`/meals/current` 只返回本人创建的进行中饭局。
- 点餐请求统一传饭局候选菜 ID，字段名为 `candidateIds`；候选菜内部关联原菜品 ID。
- 打卡不关联菜品草稿或菜品实体，保存用户输入的菜名快照和图片资源。
- 采购清单分享通常不要求登录，公共接口只校验高熵分享令牌并保持只读；发起人被禁用时立即撤销分享令牌，保留内容只允许登录且已加入的参与者查看。
- 允许被发现动作统一为 `PUT /dishes/{dishId}/discoverability`。
- 图片审核状态统一为 `pending|passed|rejected|failed|not_required`；增强能力执行状态与计费状态拆分，执行使用 `pending|processing|succeeded|failed`，计费使用 `not_charged|charged|refund_pending|refunded`。
- 从平台推荐或 AI 推荐加入个人菜品库的副本默认未公开、允许编辑，且永久不能再次设置为允许被发现。
- 推荐模块只包含菜品推荐，不包含推荐菜谱；首页展示 5 个推荐菜品并提供“查看更多”。
- 菜谱由用户从已编辑好的个人菜品中创建和维护；菜品详情不提供“加入菜谱”，菜谱详情负责添加、左滑移除和删除菜谱。
- 采购清单不做分类，支持待购买 / 已购买、勾选、编辑、新增、删除、复制和微信分享。
- “不知道吃什么”先以 SQL 执行硬条件过滤和推荐库召回，样本不足时才调用 AI；AI 必须推荐真实存在的经典菜系、常见材料和可验证做法。
- 每次打卡后可异步分析图片并更新用户菜品喜好画像；积分流水与通知列表均使用接口分页、界面无页码无限滚动，默认每页 20 条并按 ID 去重。
- 通知中心默认进入左侧“未读”页签，右侧为“全部”；全部已读只更新阅读状态，不删除历史通知。
- 小程序冷启动自动调用 `wx.login` 获取一次性 `code`，由后端兑换 `openid` 并签发业务令牌；`openid`、`session_key` 和微信接口密钥不得返回或写入小程序。
- 增强能力采用平台总开关：平台可整体关闭或开启，开启后全部能力统一开放，不提供单用户或单项能力开关；全局紧急停用优先级最高，首发默认关闭。
- 每项能力只绑定一个与模态兼容的主模型，不设置备用模型；失败或超时不自动切换模型，立即进入退款流程。
- 六项能力内置平台默认提示词；自定义提示词只编辑系统和用户模板，变量与输出 JSON Schema 固定，保存后立即生效，不提供配置草稿、发布、历史版本或回滚。
- 微信订阅只保留饭局最终结果；参与者加入后主动一次授权，关闭点餐、发起人自己的操作、用户禁用和 AI 退积分不发送。
- 小程序上传发布包不得出现相关敏感能力字样，范围包括可见文案、页面路径、接口路径、模块名、字段名、变量名、资源文件名和构建产物；客户端统一使用中性功能编码。
- 受控功能的按钮名、标题和说明由后端 `RuntimeConfig` 下发，不在小程序中写死；能力关闭时不下发对应功能项。
- 增强能力关闭时积分体系同步关闭：隐藏积分余额与流水入口，打卡仍可保存但不发放积分、不写积分流水，也不触发偏好分析；历史积分数据保留。

## 当前进展

- 已完成需求访谈和方案确认。
- 已选择“核心闭环 + 轻量推荐池”的第一版产品方案。
- 已生成中文 PRD：`aiDoc/prd/family-menu-miniapp-prd.md`。
- 已生成中文功能点思维导图：`aiDoc/prd/family-menu-miniapp-feature-mindmap.md`。
- 已生成 Mermaid 渲染 PNG：`aiDoc/prd/family-menu-miniapp-feature-mindmap.png`。
- 已补充菜品标签、管理员推荐菜谱、做菜打卡、偏好画像和“不知道吃什么”AI 推荐的阶段边界。
- 已补充打卡照片 AI 识别生成私有菜品草稿，以及阿里云图片内容同步审查接口配置。
- 早期曾讨论菜品下架状态和公开审核流程，现已被 `draft|usable`、软删除和“允许被发现，无独立审核队列”的现行口径覆盖。
- 已确认产品名“来干饭”、第一版完整范围、AI 独立配置页、产品指标、隐私协议规划和饭局边界规则。
- 已补充核心数据模型：菜品单实体、饭局快照、积分流水、AI 调用记录、打卡、可发现推荐和通知等实体关系。
- 已完成 20 个小程序页面的逐页 UI 确认与设计稿归档，猫猫插画基础色调、角色模型、线条和笔触已锁定。
- 2026-07-18：已在 `miniApp/` 生成原生微信小程序代码、统一组件、Mock 数据和请求层，覆盖 20 个已确认页面与主要交互状态。
- 2026-07-18：已生成 `aiDoc/miniApp/api.json`，按 13 个模块整理接口，并明确分页、积分退还、幂等、推荐复制锁定与通知规则。
- 2026-07-23：前端 Mock 阶段收口；补齐历史饭局独立页、远程资源映射、全局分享与采购清单专用分享封面，现阶段不再继续调整页面设计。
- 设计任务、定稿稿件和接口契约已统一归档到 `aiDoc/miniApp/`，下一阶段入口为 `aiDoc/miniApp/backend-integration-plan.md`。
- 2026-07-24：已验证 `mcp:order_food_mini_app` 可连接；数据库 API 列表读取成功，共 190 条，当前 Gin 运行时 API 为 0；菜单读取成功，共 10 个顶层节点、43 个展开节点。
- 2026-07-24：后端确定使用独立核心业务包 `orderfood`，不放入 `system`、`example`，也不采用插件形态；仅稳定 CRUD 使用 GVA MCP 生成，交易型业务手写。
- 2026-07-24：已补充后台 Web PRD、`orderfood` 包架构、GVA MCP 生成规范和跨端契约基线，并归一化封面、菜品状态、饭局状态、推荐来源锁和未定义接口模型。
- 2026-07-24：新增静默登录、平台默认策略、用户级覆盖、动态功能命名、积分联动和小程序发布包中性化基线；`api.json` 升级为 `1.2.0`，共 13 个模块、63 个接口。
- 2026-07-24：冻结采购分享免登录、候选菜 ID 点餐、打卡不绑定菜品、菜品封面必填、允许被发现路由、能力与审核状态枚举，以及单创建者单进行中饭局和手动完成状态；`api.json` 升级为 `1.3.0`，共 13 个模块、64 个接口。
- 2026-07-26：小程序机器契约升级为 `1.8.1`、66 条接口；补充发起人被禁用后已加入参与者查看保留只读采购清单的登录态接口。
- 2026-07-26：小程序机器契约升级为 `1.9.0`、67 条接口；补齐完成饭局的参与者历史采购清单只读入口，以及发起人在收集阶段按候选菜 ID 移除候选菜的完整链路。
- 2026-07-24：错误码由 `server/errors/error.go` 统一管理，前台小程序使用 `40000-49999`、后台管理使用 `20000-29999`；前台接口参数统一通过 `server/utils/front_validator.go` 的 `utils.VerifyAll` 校验。
- 2026-07-24：小程序认证客户端已实现冷启动 `wx.login`、共享认证 Promise、业务令牌与运行时配置持久化、并发 `40101` 单飞重登、安全请求重放和公开采购分享免登录；图片上传与微信一次性 code 兑换禁止自动重放。后端已实现真实 `jscode2session`、微信身份加密保存和业务令牌签发。
- 2026-07-24：小程序机器契约升级为 `1.5.0`；正式客户端已覆盖 64/64 个接口，8 条旧路径和旧 DTO 已迁移，Mock 与客户端使用同一字段模型。饭局明确区分关闭点餐与手动结束，`confirmed` 期间仍是进行中饭局。
- 2026-07-24：管理端菜单与页面功能产品稿已确认为实施基线；P02、P14、P25、P26 的接口、DTO、独立敏感权限、默认角色和访问审计已经回写生成器与机器契约。图片审核首期固定阿里云，头像、菜品封面与步骤图、长截图解析图和打卡图均强制同步审核，AI 生成图固定为 `not_required`。
- 2026-07-25：增强能力改为平台总开关，不提供单用户或单项能力开关；开启前六项能力配置必须全部就绪，开启后统一开放。六项能力保留独立模型、提示词、积分、限额和校验配置。
- 2026-07-25：冻结候选源菜品失效后的不可用记录规则、禁用饭局发起人时的系统取消与历史快照保留规则；标准菜品索引校验在新环境默认关闭，索引满足数量、引用有效性和名称别名唯一性条件后才允许保存开启。
- 2026-07-25：17 项剩余产品规则全部定稿，覆盖官方封面、推荐生命周期、供应商/模型/订阅模板状态、AI 主模型与提示词、`refund_pending`、饭局截止守卫、订阅消息和发起人禁用后的只读保留。
- 2026-07-24：管理端 P02/P25/P26 已落地首批真实接口与三张 Web 页面，统一使用外部基址 `/api/orderfood`；完成小程序用户状态、偏好画像只读查看、全部用户菜品/菜谱查询及违规处理。私有内容和画像查看会原子双写专用访问留痕与管理操作记录，状态变更、通知、违规处理、操作记录和幂等结果在同一事务中，任一关键写入失败均不返回敏感数据或保留半成品。
- 2026-07-25：四类来干饭角色仅作为 GVA/Casbin 可编辑模板，不固定 authority ID，也不在业务代码按角色名授权；自动注入的第一个管理员拥有全部当前及未来 `orderfood` 权限。
- 2026-07-25：`orderfood` 的 GORM 与数据库集成测试统一切换到 MySQL 8；测试仅使用显式的 `_test` DSN，每个测试独立建库并自动清理，不读取开发配置或回退 SQLite。
- 2026-07-25：补齐 `orderfood` 管理端与小程序端 API、Service、Request、Response 的导出符号和字段注释；全部对外处理方法补充可生成的 Swagger 契约，并增加注释规范自动化检查。
- 2026-07-26：小程序 67 条接口已全部替换 Mock，管理端 28 个页面、112 条接口、85 个权限已全部落地；配置模块统一为读取当前值后直接保存生效，不提供草稿、发布、历史版本或回滚。
- 2026-07-26：补齐阿里云图片审核生产适配器，使用官方 `green-20220302` SDK 执行临时 OSS 上传和同步审核；图片审核及 AI 供应商凭据引用统一限制为当前可执行的 `env://` 形式。
- 2026-07-26：管理端契约、Swagger、Web API、权限、小程序契约、发布包扫描、认证流程、Go 注释检查和 Web 构建均通过；管理端与小程序 `orderfood` 目标包已在隔离 MySQL 8 测试实例完成数据库事务、并发、幂等和状态机完整回归。
- 2026-07-26：P18 能力配置移除从未参与运行时执行的 `resultValidationPolicy` 可编辑 JSON；当前数据模型不再包含该字段，项目作为新项目不保留旧库升级迁移。管理端改为只读展示服务端固定校验规则，“不知道吃什么”的标准菜品索引兜底开关和失败后固定重试一次仍只在标准菜品索引页维护。
- 2026-07-26：管理端取消“来干饭”业务根目录和“工作台”中间层，数据概览替代 GVA 原仪表盘并作为一级菜单；用户与积分、菜品运营、内容安全、饭局管理、AI 能力、消息中心同样提升为一级分组。新增一级“微信配置”，AppID 与加密保存且不回显的 AppSecret 同时供微信登录和订阅消息使用。新项目初始化直接写最终菜单与权限结构，已有数据库只提供独立 SQL，不在 Go 中保留兼容迁移。
- 2026-07-26：管理端当前基线为 6 个菜单分组、数据概览与微信配置 2 个一级直达页面；后续积分调整页收敛及订阅场景改造后为 28 个页面、114 条接口和 85 个权限码；已有数据库调整脚本固定为 `aiDoc/admin/orderfood-menu-wechat-current-db.sql`。
- 2026-07-27：积分调整由独立菜单页面收敛为用户列表行操作和积分流水页头共用的轻量弹框，当前可见菜单页面为 28 个；弹框复用 `user:read` 查询用户并保留余额预览、原因与确认流程。已有数据库先通过 `waitSql/20260727_remove_point_adjustment_menu.sql` 清理独立菜单，再通过 `waitSql/20260727_add_point_entries_user_read_button.sql` 复制现有用户读取按钮授权；两份 SQL 均由用户手工执行。
- 2026-07-27：管理端列表搜索区按复杂度收敛为默认 2—3 个基础条件和按需高级筛选，时间范围加宽且展开收起无动画；已在 100% 缩放下完成小程序用户列、整体能力列、操作按钮尺寸对齐和积分调整弹框的浏览器验收。
- 2026-07-28：订阅消息管理由可自由新增、编辑和删除模板改为代码固定场景目录；第一版仅 `meal_status`，管理员只维护微信模板 ID 与 `mealName`、`result`、`resultAt` 三个固定字段映射，首次绑定默认停用，后续原位更新并独立启停。
- 2026-07-28：模型管理页的新增和编辑入口只依赖本页 `model:create`、`model:update` 按钮权限，不再从当前路由按钮元数据中跨菜单检查 `provider:read`；模型页专用供应商基础选项和实时模型目录接口统一由后端校验 `model:read`。
- 2026-07-28：模型管理新增和编辑改为先选择供应商，再由后端使用安全凭据实时读取供应商 `/models` 接口并下拉选择；已配置项置灰，加载失败只允许重试，不再提供自由文本模型标识输入。

## 后续待办

- 在具备真实微信、AI 供应商和阿里云 RAM 凭据的测试环境完成外部服务联调；凭据不得写入仓库或数据库。
- 上线前继续以管理端、小程序机器契约和跨端契约基线作为变更唯一入口，避免页面、Swagger 和实现各自漂移。
- 管理端路由、DTO、权限码、角色模板和专属错误码以 `aiDoc/admin/api.json` 与 `aiDoc/frontend-backend/order-food-admin-api-contract.md` 为准。
- `aiDoc/prd/order-food-admin-menu-page-review.md` 的 G01—G41 已全部定稿并完成代码验收；实现按已回写的主 PRD 和机器契约执行。
- 登录、能力策略、积分联动和发布包扫描规则以 `aiDoc/frontend-backend/miniapp-auth-capability-compliance.md` 为准。
- 在测试环境对关键闭环执行带真实外部凭据的联调验收：微信登录、图片上传审核、AI 调用，以及其后的菜品录入、推荐复制、菜谱、饭局点菜、采购清单、打卡、积分与通知。
- 发布前再统一执行一次真机安全区、系统胶囊、分享卡片和资源域名回归，不在后端阶段重新设计页面。

## 更新规则

- 同一需求始终维护在本文件中。
- 新信息优先补充到对应段落，不另起重复记录。
- 只有需求状态变化时，才在 `active/` 与 `done/` 之间移动文件。
